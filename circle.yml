machine:
  node:
    version: 5.0.0
dependencies:
  pre:
    - npm install
deployment:
  production:
    tag: $CIRCLE_BUILD_NUM
    commands:
      - ./ci/npm-login.sh
      - npm publish --registry https://gcartifactory-us.jfrog.info/artifactory/api/npm/circleci-npm-local