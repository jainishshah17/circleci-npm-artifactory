machine:
  node:
    version: 5.0.0
dependencies:
  pre:
    - npm install
deployment:
  master:
    branch: master
    commands:
      - ./npm-login.sh
      - npm publish --registry https://gcartifactory-us.jfrog.info/artifactory/api/npm/circleci-npm-local
